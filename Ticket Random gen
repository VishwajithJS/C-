import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter starting serial number: ");
        int start = sc.nextInt();
        System.out.print("Enter ending serial number: ");
        int end = sc.nextInt();
        System.out.print("Enter number of tickets to select: ");
        int k = sc.nextInt();
        if (start > end || k > (end - start + 1)) {
            System.out.println("Invalid input");
            return;
        }
        ArrayList<Integer> tickets = new ArrayList<>();
        for (int i = start; i <= end; i++) {
            tickets.add(i);
        }
        Collections.shuffle(tickets);
        System.out.println("Selected tickets:");
        for (int i = 0; i < k; i++) {
            System.out.print(tickets.get(i) + " ");
        }
    }
}
